version: 1.0
runtime: nodejs16
build:
  commands:
    build:
      - cd backend
      - yarn install
      - yarn build
run:
  command: yarn start
  network:
    port: 5000
  env:
    - name: BASE_URL
      value: "http://localhost:5000"